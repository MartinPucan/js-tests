export function sum(a, b) {
    return a + b
}

const subtract = (a, b) => a - b

export default subtract();
// const divide = (a, b) => a / b
// const multiplication = (a, b) => a * b

// module.exports = { subtract, divide, multiplication }
